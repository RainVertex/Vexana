# ${{ values.name | pascalCase }}Widget

Home-page widget `${{ values.name }}`.

The scaffolder generates the component file. Wiring the widget into the home
layout is still done by hand for now (registering it under
`apps/web/src/widgets/registry.tsx`).
