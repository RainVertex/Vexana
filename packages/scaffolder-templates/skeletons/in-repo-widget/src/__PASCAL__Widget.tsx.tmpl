export function ${{ values.name | pascalCase }}Widget() {
  return (
    <div className="h-full flex items-center justify-center text-sm text-app-text-muted">
      ${{ values.name | pascalCase }} widget
    </div>
  );
}
