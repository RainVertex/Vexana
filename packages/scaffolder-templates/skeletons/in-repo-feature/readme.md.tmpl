# ${{ values.name }}

Feature module for `${{ values.name }}`.

- `backend/` — Express router mounted at `/api/${{ values.name }}`
- `frontend/` — React page at `/${{ values.name }}`

The scaffolder wires both into `apps/api` and `apps/web` automatically and
adds a sidebar entry. Re-running the same template against this feature is a
no-op; if the template version changes, the drift inbox surfaces an updatable
plan.
