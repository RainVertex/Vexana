import { Router } from "express";

export const ${{ values.name | camelCase }}Router: Router = Router();

${{ values.name | camelCase }}Router.get("/", (_req, res) => {
  res.json({ items: [] });
});
